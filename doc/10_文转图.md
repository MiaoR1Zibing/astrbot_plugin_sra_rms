---
title: "10 - 文转图"
date: 2026-06-17
description: AstrBot HTML/Jinja2模板渲染图片功能详解
---

# 文转图

> 为了方便开发，您可以使用 [AstrBot Text2Image Playground](https://t2i-playground.astrbot.app/) 在线可视化编辑和测试 HTML 模板。

## 目录

- [基本](#基本)
- [自定义(基于 HTML)](#自定义-基于-html)

---

## 基本

AstrBot 支持将文字渲染成图片。

```python
@filter.command("image") # 注册一个 /image 指令，接收 text 参数。
async def on_aiocqhttp(self, event: AstrMessageEvent, text: str):
    url = await self.text_to_image(text) # text_to_image() 是 Star 类的一个方法。
    # path = await self.text_to_image(text, return_url = False) # 如果你想保存图片到本地
    yield event.image_result(url)
```

---

## 自定义(基于 HTML)

如果你觉得上面渲染出来的图片不够美观，你可以使用自定义的 HTML 模板来渲染图片。

AstrBot 支持使用 `HTML + Jinja2` 的方式来渲染文转图模板。

```python
# 自定义的 Jinja2 模板，支持 CSS
TMPL = '''
<div style="font-size: 32px;">
    <h1 style="color: black">Todo List</h1>
    <ul>
        {% for item in items %}
        <li>{{ item }}</li>
        {% endfor %}
    </div>
'''

@filter.command("todo")
async def custom_t2i_tmpl(self, event: AstrMessageEvent):
    options = {} # 可选择传入渲染选项。
    url = await self.html_render(TMPL, {"items": ["吃饭", "睡觉", "玩原神"]}, options=options)
    yield event.image_result(url)
```

返回的结果:

这只是一个简单的例子。得益于 HTML 和 DOM 渲染器的强大性，你可以进行更复杂和更美观的设计。除此之外，Jinja2 支持循环、条件等语法以适应列表、字典等数据结构。

---

## 图片渲染选项(options)

请参考 Playwright 的 [screenshot](https://playwright.dev/python/docs/api/class-page#page-screenshot) API。

| 选项 | 类型 | 说明 |
|------|------|------|
| `timeout` | float | 截图超时时间 |
| `type` | Literal["jpeg", "png"] | 截图图片类型 |
| `quality` | int | 截图质量，仅适用于 JPEG 格式图片 |
| `omit_background` | bool | 是否允许隐藏默认的白色背景（透明图），仅适用于 PNG |
| `full_page` | bool | 是否截整个页面而不是仅设置的视口大小，默认为 True |
| `clip` | dict | 截图后裁切的区域 |
| `animations` | Literal["allow", "disabled"] | 是否允许播放 CSS 动画 |
| `caret` | Literal["hide", "initial"] | 截图时是否隐藏文本插入符号，默认为 hide |
| `scale` | Literal["css", "device"] | 页面缩放设置。css 将设备分辨率与 CSS 中的像素一一对应 |

---

**文档信息**

- 原始文档: https://docs.astrbot.app/dev/star/guides/html-to-pic.html
- 整理时间: 2026-06-17
